{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Lagrange–Chebyshev manifests",
  "description": "Metadata manifest for a set of Lagrange–Chebyshev Libration Point fit records.",
  "type": "object",
  "required": ["source", "ncoeff", "bodies", "block_seconds"],
  "properties": {
    "source": { "type": "string" },
    "ncoeff": {
      "type": "integer",
      "description": "Number of Chebyshev coefficients per component per block."
    },
    "bodies": {
      "type": "array",
      "description": "List of body names corresponding to each .f64le data file.",
      "items": { "type": "string" }
    },
    "jd_start": { "type": "number", "description": "Julian date of first block start." },
    "jd_end": { "type": "number", "description": "Julian date of last block end." },
    "block_seconds": { "type": "number", "description": "Duration of each Chebyshev block in seconds." },
    "max_fit_error_m": { "type": "number", "description": "Maximum fit residual in metres." },
    "rms_fit_error_m": { "type": "number", "description": "RMS fit residual in metres." }
  }
}
